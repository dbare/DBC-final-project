<!DOCTYPE html>
<html>
  <head>
    <title>CodeConnect</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/tether/1.4.0/js/tether.min.js"></script>

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  	<nav class="nav nav-tabs" style="background-color: black;">
      <div>
        <ul>
          <% if current_user %>
            <div class="nav-bar-container row">
              <div class="company-link btn"><%= link_to "User Profile", user_path(current_user) %></div>
              <div class="company-link btn"><%= link_to "Log Out", logout_path %></div>
              <div class="company-link btn"><%= link_to "Home", root_path %></div>
            </div>
          <% else %>
            <div class="nav-bar-container row">
              <div class="nav-bar-btn btn"><%= link_to "Login", login_path %></div>
              <div class="nav-bar-btn btn"><%= link_to "Register", new_user_path %></div>
              <div class="nav-bar-btn btn"><%= link_to "Home", root_path %></div>
            </div>
          <%end%>
        </ul>
      </div>
  	</nav>
    <div>
      <% flash.each do |key, value| %>
        <div style="color:red;" class="alert <%= key %>"><%= value %></div>
      <% end %>
    </div>
    <%= yield %>
    <div class="text-center">
      <center><footer><div id="layout-footer" class="text-center">
        <%= link_to "About", root_path %>  |  <%= link_to "Contact", contact_path %>  |  <%= link_to "Team", team_path %>
      </div></footer></center>
    </div>
  </body>
</html>
