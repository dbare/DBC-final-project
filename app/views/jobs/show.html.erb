<h1> Job ID: <%=@job.id%>  </h1><br>

<div class="text-center">
	<p><h4>Position</h4> <small><%= @job.position %></small></p><br>
	<% if @job.company%>
		<p><h4>Company</h4> <small><%= @job.company.name %></small></p><br>
	<% end %>
	<p><h4>Location</h4> <small><%= @job.location %></small></p><br>
	<p><h4>Description</h4> <small><%= @job.description %></small></p><br>
</div>

<% if current_user.company_id == @job.company_id %>
	<%= link_to "Update this Listing", edit_job_path(@job) %> 
	<%= link_to "Delete this Listing", job_path(@job), method: :delete %>
<% end %>

<% if @user_favorite %>
	<button class="btn btn-lg btn-primary"><%= link_to "Unfavorite", favorite_path(@user_favorite), method: :delete %></button>
<% else %>
	<%= form_for @favorite do |f| %>
			<%= f.hidden_field :job_id , :value => @job.id%>
			<%= f.hidden_field :user_id , :value => @user.id%>
			<%= submit_tag "Favorite", :class => "btn btn-lg btn-primary"  %>
	<% end %>
<% end %> 
